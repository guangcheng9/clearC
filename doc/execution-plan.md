# 执行文档

本文件用于指导实际开发。版本目标参考 [版本路线图](./roadmap.md)，安全边界参考 [安全策略](./safety-policy.md)。执行时必须同步当前版本、里程碑、验证结果和相关文档。

## 当前执行目标

当前进入 `V2：可控迁移能力`。

- `V0`：已完成工程初始化、页面框架、规则系统、Native Core 基础结构。
- `V1`：已完成只读扫描、安全清理、日志回滚、开发工具只读扫描和发布准备。
- `V1.1`：已完成可用性修复，长任务已后台化并补充 loading / 禁用原因。
- `V2`：已开始，`M8-M9` 用户目录迁移执行与回滚代码加固已完成，待真实用户目录手动验收；下一步进入 `M10` 开发工具环境变量迁移。

## 执行同步约定

每次执行开发任务后，都需要同步：

- 当前执行到哪个版本、里程碑和步骤，例如 `V2 / M10 / Cargo 环境变量迁移`。
- 本次完成了哪些改动。
- 本次是否修改了系统行为、安全策略、规则结构或页面流程。
- 本次验证了哪些命令，验证结果是什么。
- 是否存在需要用户确认的问题。
- 是否已经同步相关文档。
- 如果变更影响用户可见功能、按钮流程、结果展示、错误提示或安全提示，必须同步 `user-manual.md`。

如果实现内容影响产品范围、版本计划、安全边界、架构或开发流程，必须同步更新对应文档：

| 影响范围 | 需要同步 |
|---|---|
| 产品功能和边界 | `product-plan.md`、`roadmap.md` |
| 版本和里程碑 | `roadmap.md`、`execution-plan.md` |
| 删除、迁移、权限、回滚 | `safety-policy.md` |
| 开发工具和 AI 工具迁移 | `devspace-manager.md` |
| 技术栈、目录结构、打包方式 | `architecture.md` |
| 启动、构建、环境、协作流程 | `development-setup.md` |
| 打包发布 | `release.md` |
| 用户可见操作流程、正确/错误结果 | `user-manual.md` |

## 总里程碑

| 版本 | 里程碑 | 目标 | 状态 |
|---|---:|---|---|
| V0 | M0 | 项目初始化 | 已完成 |
| V0 | M1 | 规则系统 | 已完成 |
| V1 | M2 | 扫描系统 | 已完成 |
| V1 | M3 | 安全清理 | 已完成 |
| V1 | M4 | 日志回滚 | 已完成 |
| V1 | M5 | 用户目录只读检测和预检查 | 已完成 |
| V1 | M6 | 开发工具只读扫描 | 已完成 |
| V1 | M7 | 打包发布准备 | 已完成，安装包受 NSIS 下载超时阻塞 |
| V1.1 | M7.1 | 可用性修复 | 已完成 |
| V2 | M8 | 用户目录迁移执行 | 代码加固已完成，待真实手动验收 |
| V2 | M9 | 用户目录迁移回滚 | 代码加固已完成，待真实手动验收 |
| V2 | M10 | 开发工具环境变量迁移 | 待做 |
| V2 | M11 | Junction 迁移预案 | 待做 |
| V2 | M12 | 任务体验增强 | 待做 |
| V3 | M13 | 大文件高级分析 | 待做 |
| V3 | M14 | 大目录树形分析 | 待做 |
| V3 | M15 | 重复文件检测 | 待做 |
| V3 | M16 | 扫描历史和智能建议 | 待做 |
| V4 | M17 | 安装包发布完善 | 待做 |
| V4 | M18 | 便携版 zip | 待做 |
| V4 | M19 | 规则版本管理和导入导出 | 待做 |
| V4 | M20 | 崩溃日志和诊断 | 待做 |
| V4 | M21 | 应用内更新提示 | 待做 |
| V5 | M22 | Docker / WSL / 虚拟机只读分析 | 待做 |
| V5 | M23 | Android 模拟器和游戏库分析 | 待做 |
| V5 | M24 | 企业策略配置 | 待做 |

## V0：工程基础

### M0：项目初始化

验收：

- 应用可以本地启动。
- Windows 桌面窗口可以打开。
- 页面能在 Dashboard、Cleanup、Migration、DevSpace、Analysis、Logs 间切换。
- 前端可以调用 Rust command。
- Rust 后端 `cargo check` 通过。
- 前端 `npm run build` 通过。

### M1：规则系统

验收：

- Rust 能读取 `rules/cleanup.rules.json`、`rules/migration.rules.json`、`rules/devspace.rules.json`。
- 规则结构能反序列化为类型化数据。
- 前端可以通过 `get_rule_catalog` 获取规则目录。
- Cleanup、Migration、DevSpace 页面展示来自规则文件的数据。
- 规则读取失败时，前端能展示错误信息。

## V1：安全清理 MVP

### M2：扫描系统

验收：

- Rust 能解析 `%USERPROFILE%`、`%TEMP%`、`$Recycle.Bin` 等规则路径。
- Rust 能读取系统盘总容量、可用容量和已用容量。
- Rust 能对规则命中的路径做只读递归统计。
- 扫描结果包含存在性、可访问性、文件数量、目录数量、占用大小和跳过数量。
- Dashboard 页面展示系统盘概览。
- Analysis 页面展示规则扫描结果。
- 扫描阶段不执行删除、迁移或配置修改。

### M3：安全清理

验收：

- 清理预览能基于清理规则生成预计释放空间。
- 清理预览能展示文件数量、跳过数量、路径明细和风险等级。
- 清理预览默认要求二次确认。
- Cleanup 页面可以记录清理计划草稿。
- 第一版真实清理只允许 `temp-user` 隔离移动，不允许永久删除。
- 隔离清理结果必须记录移动数量、跳过数量、隔离目录和失败原因。

### M4：日志回滚

验收：

- 隔离清理日志记录 `movedItems`，包含原路径和隔离路径。
- Logs 页面能识别可回滚的 `cleanup-quarantine` 记录。
- 回滚命令能把隔离文件恢复到原路径。
- 原路径已存在或隔离文件缺失时，回滚必须跳过并记录失败。
- 同一个隔离清理操作不能重复回滚。
- 回滚操作会写入新的日志记录。

### M5：用户目录只读检测和预检查

验收：

- 迁移模块能读取 Windows `User Shell Folders` 当前配置。
- 支持检测桌面、下载、文档、图片、视频、音乐。
- 页面展示配置路径、解析后路径、是否存在、是否仍在系统盘。
- 迁移预检查能评估目标盘是否存在、目标目录是否冲突、目标是否仍在系统盘、目标空间是否足够。
- 预检查阶段不创建目录、不写文件、不修改注册表。

### M6：开发工具只读扫描

验收：

- DevSpace 规则覆盖 Android、Cargo、Bun、通用缓存、Chromium snapshots、AI 工具、Apifox、CodeBuddy、AWS。
- DevSpace 扫描能统计路径存在性、文件数量、占用空间和跳过项。
- DevSpace 页面展示风险等级、分类、推荐动作和迁移方式。
- 当前阶段只读扫描，不清理、不迁移、不修改环境变量、不创建 Junction。

### M7：打包发布准备

验收：

- Tauri 打包配置包含 Windows 安装包目标。
- `package.json` 提供桌面开发和生产打包脚本。
- 发布文档说明安装包、MSI 和便携版策略。
- 发布前检查项覆盖构建、格式、安全边界和文档同步。
- `npm run tauri:build -- --no-bundle` 已通过。
- 当前 release exe 已生成，NSIS/MSI 安装包因 NSIS 下载超时阻塞。

## V1.1：可用性修复

### M7.1：长任务与按钮状态

验收：

- 长耗时命令必须放入后台线程，避免窗口显示未响应。
- 扫描、预览、隔离清理、迁移预检查、回滚都必须显示 loading 状态。
- 有前置条件的按钮必须显示禁用原因。
- 禁用按钮不能让用户误以为应用卡死或按钮失效。

## V2：可控迁移能力

### M8：用户目录迁移执行

当前状态：

- 代码加固已完成。
- 已打通前端入口、Rust command、迁移计划、执行结果和操作日志。
- 已补充跨盘移动 fallback、注册表值类型保留、失败日志和前端确认短语。
- 尚未对当前机器真实用户目录执行迁移验收。

已完成：

- Migration 页面支持生成迁移计划。
- `create_user_folder_migration_plan` 已接入预检查。
- `execute_user_folder_migration` 已接入确认短语、目标目录创建、直接子项移动、`User Shell Folders` 写入和日志记录。
- 文件移动会先尝试系统重命名；失败时使用复制、大小校验、删除源文件的受控 fallback。
- 注册表写入会尽量保留原值类型，默认使用 `REG_EXPAND_SZ`。
- 注册表写入失败会进入迁移日志，不再让文件已移动但无审计记录。
- 前端执行前需要输入 `MIGRATE_USER_FOLDER`，并通过确认弹窗展示源路径和目标路径。
- 执行结果可以展示移动项和失败项。
- 迁移日志记录原注册表值、目标注册表值、原路径、目标路径、移动项和失败项。

剩余未完成：

- 需要补充真实迁移手动验收记录。开发和自动测试阶段不得迁移当前机器真实用户目录。
- 需要执行后校验，确认注册表、目标目录、移动项和失败项状态一致。
- 需要 Explorer 刷新或提示用户重新登录/重启资源管理器，否则系统默认目录显示可能延迟更新。
- 需要补充进度事件、取消能力和失败项导出，这部分归入 `M12`。
- 需要补充更完整的集成测试，包括日志字段完整性、注册表写入失败、跨盘复制失败。

验收：

- Migration 页面可以基于预检查生成用户目录迁移计划。
- 执行迁移前必须通过预检查，并要求确认短语。
- 执行迁移会创建目标目录，移动源目录直接子项，并写入 `User Shell Folders`。
- 迁移日志必须记录原注册表值、目标注册表值、原路径、目标路径、移动项和失败项。
- 迁移失败或部分失败时，不删除原目录、不覆盖目标已有文件。

### M9：用户目录迁移回滚

当前状态：

- 代码加固已完成。
- 已打通 Logs 页面入口、Rust command、回滚日志和重复回滚拒绝。
- 已补充跨盘回滚 fallback、注册表恢复失败日志和回滚确认弹窗。
- 尚未对当前机器真实用户目录执行回滚验收。

已完成：

- Logs 页面能识别 `user-folder-migration` 日志并调用迁移回滚命令。
- `rollback_user_folder_migration` 能根据迁移日志恢复文件项和注册表原值。
- 回滚文件恢复会复用受控移动流程，支持跨盘复制、校验、删除源文件。
- 回滚会跳过原路径冲突、目标文件缺失等失败项，并记录结果。
- 注册表恢复失败会进入回滚日志，并展示 `rolled-back-with-registry-failure` 状态。
- Logs 页面回滚前会弹出确认提示，并区分“回滚迁移”和“回滚清理”。
- 同一个迁移操作重复回滚会被拒绝。
- 回滚操作会写入新的不可回滚日志。

剩余未完成：

- 需要补充真实回滚手动验收记录。开发和自动测试阶段不得回滚当前机器真实用户目录。
- 需要区分“部分回滚成功”和“完全回滚成功”的状态，并在 Logs 页面明确展示。
- 需要把可回滚日志列表做完整展示、筛选和详情页，当前页面只展示最近操作。
- 需要支持失败项导出，方便用户手动处理被占用或冲突文件，这部分归入 `M12`。
- 需要补充缺失日志、日志字段缺失、原注册表值为空等测试。

验收：

- Logs 页面能识别可回滚的 `user-folder-migration` 记录。
- 回滚迁移会逐项把文件从目标路径恢复到原路径。
- 回滚会恢复 `User Shell Folders` 原值。
- 原路径已存在或目标文件缺失时，回滚必须跳过并记录失败。
- 同一个迁移操作不能重复回滚。
- 回滚操作会写入新的不可回滚日志记录。

### M10：开发工具环境变量迁移

目标：

- 为 Cargo / Rustup / Android 等支持环境变量的工具生成迁移计划。
- 修改用户级环境变量。
- 记录原环境变量值和新值。
- 提供环境变量回滚。

验收：

- 支持 `CARGO_HOME`、`RUSTUP_HOME`、`ANDROID_USER_HOME`。
- 迁移前必须展示原值、新值和目标路径。
- 修改环境变量必须写入日志。
- 回滚能恢复原环境变量值，原值不存在时能删除新增变量。
- `.aws` 等凭据目录不允许自动迁移。

### M11：Junction 迁移预案

目标：

- 为不支持配置路径但适合迁移的目录提供 Junction 迁移预案。
- 第一阶段先实现检测和计划，执行创建 Junction 需要二次确认。

验收：

- 支持检测源路径、目标路径、目标盘空间、已有 Junction。
- 支持展示 Junction 风险说明。
- 创建 Junction 前必须二次确认。
- Junction 回滚必须先检测目标状态。
- 高风险目录默认只读，不显示执行按钮。

### M12：任务体验增强

目标：

- 让扫描、迁移、回滚等长任务具备更清晰的任务状态。
- 支持取消和失败项导出。

验收：

- 长任务支持进度事件。
- 扫描/迁移任务支持取消。
- 失败项可以导出。
- 页面展示任务队列状态。
- 用户离开页面再返回时能看到最近任务结果。

## V3：深度分析

### M13：大文件高级分析

目标：

- 扫描系统盘大文件。
- 支持按大小、类型、修改时间筛选。

验收：

- 默认只展示，不删除。
- 支持排除系统关键目录。
- 支持打开文件所在目录。

### M14：大目录树形分析

目标：

- 展示系统盘目录占用树。
- 帮助用户定位空间来源。

验收：

- 支持目录层级展开。
- 支持跳过权限不足目录并记录。
- 支持按大小排序。

### M15：重复文件检测

目标：

- 检测用户目录中的重复文件。
- 第一阶段限制在下载、文档、图片、视频等用户目录。

验收：

- 重复文件默认只展示，不自动删除。
- 支持按大小和文件类型筛选。
- 删除能力若上线，必须复用隔离策略。

### M16：扫描历史和智能建议

目标：

- 保存扫描历史。
- 提供空间变化趋势和清理/迁移建议。

验收：

- 能展示最近扫描结果。
- 能对比空间变化。
- 建议必须说明原因、风险和推荐动作。

## V4：产品化发布

### M17：安装包发布完善

目标：

- 解决 NSIS/MSI 工具链下载或缓存问题。
- 稳定生成 Windows 安装包。

验收：

- `npm run tauri:build` 能生成 NSIS 安装包。
- MSI 打包可用或明确记录阻塞原因。
- 发布文档记录产物路径和安装验证步骤。

### M18：便携版 zip

目标：

- 生成绿色便携版。

验收：

- 可从 release exe 和必要资源生成 `ClearC_Portable.zip`。
- 解压后可运行。
- 本地日志和隔离区仍写入 `.clearc`。

### M19：规则版本管理和导入导出

目标：

- 规则文件支持版本号。
- 支持导入导出规则。

验收：

- 规则版本可展示。
- 导入规则前能校验结构。
- 导入失败不覆盖原规则。

### M20：崩溃日志和诊断

目标：

- 收集本地崩溃和错误诊断信息。

验收：

- 崩溃日志写入本地文件。
- 日志不包含敏感路径内容之外的凭据。
- 页面提供诊断日志位置。

### M21：应用内更新提示

目标：

- 支持版本检查和更新提示。

验收：

- 当前版本可展示。
- 能展示新版本提示。
- 不强制自动更新。

## V5：高级场景

### M22：Docker / WSL / 虚拟机只读分析

目标：

- 对高风险专业数据先做只读分析。

验收：

- 支持 Docker 数据目录只读识别。
- 支持 WSL 发行版空间分析。
- 支持虚拟机镜像大小分析。
- 不自动迁移、不自动删除。

### M23：Android 模拟器和游戏库分析

目标：

- 覆盖高占用但需要谨慎处理的开发/娱乐数据。

验收：

- 支持 Android 模拟器镜像分析。
- 支持常见游戏库目录分析。
- 只读展示，迁移能力另行评估。

### M24：企业策略配置

目标：

- 提供企业环境下的功能开关和安全策略。

验收：

- 可禁用真实清理。
- 可禁用迁移执行。
- 可限制只读扫描范围。
- 策略配置有文档说明。

## 每次开发前需要读取哪些文档

| 开发内容 | 必读 | 可选 |
|---|---|---|
| 页面和交互 | `architecture.md`、`execution-plan.md` | `product-plan.md` |
| 扫描和清理 | `safety-policy.md`、`execution-plan.md` | `roadmap.md` |
| 用户目录迁移 | `safety-policy.md`、`execution-plan.md` | `architecture.md` |
| 开发工具迁移 | `devspace-manager.md`、`safety-policy.md` | `roadmap.md` |
| 打包发布 | `architecture.md`、`roadmap.md`、`release.md` | `execution-plan.md` |

如果开发内容会改变用户实际操作方式、按钮可用条件、结果状态、错误提示或安全提示，还必须读取并同步 `user-manual.md`。
